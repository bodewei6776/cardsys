=render :partial => "layouts/errors", :locals => { :target => @department}
= form_for(@department, :html => { :class => "form-horizontal" }) do |f|
  - control_helper(:name, "部门名称") do
    = f.text_field :name
  
  .form-actions
    = f.submit "提交", :class => "btn"
