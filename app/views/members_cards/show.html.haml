%h1 会员卡信息
.listcontent_w.clearfix.cleft
  %ul.clearfix
  %fieldset.hidden
    %legend 属性信息
    %li.cleft
      .w1 会员名称：
      .w2
        = @members_card.member.name
        - if @members_card.member.name.blank?
          %a.blank_element
      .w1 会员卡号：
      .w2
        = @members_card.card_serial_num
    %li.cleft
      .w1 注册日期：
      .w2
        =@members_card.created_at.to_chinese_ymd
      .w1 授权人：
      .w2
        = @members_card.granters
    %li.cleft
      .w1 卡余额/卡余次：
      .w2
        = @members_card.left_fee_value
      .w1 会员卡状态：
      .w2
        = @members_card.state_desc
