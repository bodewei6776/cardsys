.w96
  .r_bt
    .r_bt_l
      .r_bt_l_a
      .r_bt_l_c.fb.white 
    .r_bt_r
      =render :partial => "layouts/errors", :locals => { :target => @common_resource_detail}
    = form_for([@common_resource, @common_resource_detail]) do |f|
      .fform
        .fformbox
          .fformbox_bt
            %p 
          .fformbox_nr
            %ul
              %li
                .w1 值：
                .w2
                  = f.text_field :detail_name
            %ul
              %li
                = f.submit "提交"

  .clear
